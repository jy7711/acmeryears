#include <bits/stdc++.h>
using namespace std;
int main()
{
    /*for (int i = 0; i < a.size(); i++) {
        cout << a[i] << endl; 
    }
    迭代器遍历
    for (vector<int>::iterator it = a.begin(); it != a.end(); ++it)
        cout << *it << endl;
    for (vector<int>::iterator it = a.rbegin(); it != a.rend(); ++it)
        cout << *it << endl; 反序输出
    */
    //迭代器减迭代器 ： 迭代器距离 
    //prev(it) : 返回it前一个迭代器  next(it) : 返回it后一个迭代器
    //如无必要，别用迭代器操作容器
    //for (auto &a : b) {对a操作会改变b的值}
    //for (auto a : b) {读取b不改变b的值}
}