#include <bits/stdc++.h>
using namespace std;
int main()
{
    //二进制基本操作
    // &  与：两个都为1时结果为1
    // |  或：至少一个为1时结果为1
    // ^  异或：不同时为1，相同时为0
    // ~  取反：0变1，1变0
    // << 左移：相当于乘以2
    // >> 右移：相当于除以2

    //常用技巧
    // 1. 判断奇偶
    // if (n & 1) {奇数} 
    // else {偶数}
    // 2. 判断是否是2的幂
    // bool isPowerOfTwo(int n) {
    //     return n > 0 && (n & (n - 1)) == 0;
    // eg. 4 0000 0100
    //     3 0000 0011
    // }
    // 3. 获取最低位的1
    //int lowbit = n & -n;
    // 4. 消除最低位的1
    //n = n & (n - 1);
    // 5. 交换两个数
    // a ^= b;
    // b ^= a;
    // a ^= b;

    //bitset 感觉很好用，
    // 但是好像应用范围比较局限，用于统计1 0 个数，可统计负数(补码)
    /*bitset<64> b(n); 64位二进制数，将输入的数转为二进制存储在b里
    注意 输入为逆序 eg. n = 1
    1的二进制       0000 0001
    bitset从头到尾  1000 0000
    bitset不能用auto 用古法 i = 0 --> n
    实际上， b是一个值为n的整数 ，可直接对b 进行位运算 b & 1
    统计个数是 b[i] == 1 不是 '1'
    */
    return 0;
}