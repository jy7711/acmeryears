#include <bits/stdc++.h>
using namespace std;
using ll = long long;
void ac()
{
    ll n, h; cin >> n >> h;
    vector<int> a(n+2, 0), d(n+2, 0);
    for (int i = 1; i <= n; i++) cin >> a[i];
    
    
    
    return;
}
int main()
{
    ios::sync_with_stdio(false), cin.tie(0);
    int t; cin >> t;
    while (t--) ac();
    return 0;
}